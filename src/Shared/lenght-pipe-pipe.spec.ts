import { LenghtPipePipe } from './lenght-pipe-pipe';

describe('LenghtPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LenghtPipePipe();
    expect(pipe).toBeTruthy();
  });
});
