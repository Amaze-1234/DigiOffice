import { KeySearchPipe } from './key-search-pipe';

describe('KeySearchPipe', () => {
  it('create an instance', () => {
    const pipe = new KeySearchPipe();
    expect(pipe).toBeTruthy();
  });
});
